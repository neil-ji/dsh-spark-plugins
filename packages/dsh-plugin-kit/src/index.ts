/**
 * dsh-plugin-kit host 侧工具。
 * 当前为空壳；随 dsh-finance / dsh-connector 迁入，逐步提取：
 *  - typert remote 装配（$mount + reflect 读取）
 *  - http 路由注册、storage domain 模式
 */
export const pluginKitVersion = '0.1.0'
